import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No need to specify turbo: false; just don't use it
  // Add any other config options here if needed
};

export default nextConfig;
